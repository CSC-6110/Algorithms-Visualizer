language: python
python:
  - "3.5"
  - "3.6"      # current default Python on Travis CI
  - "3.7"
# whitelist
branches:
  only:
    - Algorithms-Visualizer-dev
    - master
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pyb